<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SportAndroid" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/teal_700</item>
        <!-- Secondary brand color. -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColor">@color/color_333</item>
        <item name="editTextColor">@color/color_333</item>
        <item name="android:textColorHint">@color/color_999</item>
        <item name="android:textColorSecondary">@color/color_333</item>
        <item name="android:colorButtonNormal">@color/teal_700</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <!-- Customize your theme here. -->
    </style>
</resources>